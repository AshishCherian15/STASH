<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      XML theme used only for the window/splash period before Compose draws its first frame.
      windowBackground set to ink navy so the window phase matches the branded Compose splash.
      Compose MaterialTheme takes over from here.
    -->
    <style name="Theme.Stash" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/ink_navy</item>
        <item name="android:statusBarColor">@color/ink_navy</item>
        <item name="android:navigationBarColor">@color/ink_navy</item>
    </style>

    <style name="Theme.Stash.Transparent" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
